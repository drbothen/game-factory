target/
.DS_Store
node_modules/

# VSDD factory artifacts live on the factory-artifacts orphan branch (worktree)
.factory/

# Brownfield reference repos (external; ingested in Phase 0, not tracked)
.reference/
