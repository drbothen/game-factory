---
document_type: behavioral-contract
level: L3
version: "1.1"
status: draft
producer: product-owner
timestamp: 2026-06-09T00:00:00Z
phase: 1a
inputs:
  - .factory/specs/domain-spec/capabilities.md
  - .factory/specs/domain-spec/invariants.md
  - .factory/specs/architecture/adrs/ADR-0008-anti-cheat-provider-policy.md
  - .factory/planning/research/aaa/AAA-RECONCILIATION.md
input-hash: "[compute via bin/compute-input-hash at pipeline ingest]"
traces_to: domain-spec/L2-INDEX.md
origin: greenfield
subsystem: SS-11
capability: CAP-013
priority: P2
lifecycle_status: active
introduced: v0.1.0-prd-pass-42
modified:
  - version: "1.1"
    date: 2026-06-09
    author: product-owner
    reason: "F44-01: remove non-schema competitive_multiplayer trigger; gate solely on genre-profile.esports_enabled: true (schema-valid per BC-13.01.001); competitive_multiplayer has no distinct concept in the genre-profile schema and no competitive-non-esports case exists in the domain model"
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# BC-13.02.006: Anti-Cheat Integration Adapter — Allowed-Provider Enforcement and Kernel-Anomaly Rejection

## Description

Games with the esports lane active (`genre-profile.esports_enabled: true`) must declare a
conformant anti-cheat provider before D-SEC passes. The declared provider must be in
the factory-allowed set `{eac, eos, battleye}` (case-insensitive) per ADR-0008. Any
kernel-anomaly provider (specifically Riot Vanguard and its variants: `vanguard`,
`riot-vanguard`, `vgk`, `riot_vanguard`) is unconditionally rejected with
`E-ANTICH-002` and blocks scaffolding generation. The factory generates
wrap-only integration scaffolding; it never authors the provider service binary.
Generated scaffolding must pass the BC-1.15.002 lint check (no ring-0 driver artifacts).

The esports lane is the canonical genre-profile signal for competitive online multiplayer
requiring anti-cheat enforcement (per BC-13.01.001 schema field set). The genre-profile
schema does not define a separate `competitive_multiplayer` flag; `esports_enabled` is
the single schema-valid competitive-lane activation signal.

## Preconditions

1. The genre profile (validated per BC-13.01.001) declares `esports_enabled: true`
   (esports/competitive lane active). This is the sole genre-profile schema-valid trigger
   for the anti-cheat gate; `competitive_multiplayer` is not a defined schema field.
2. An `anti_cheat_config` document is present in the game artifact set, declaring at
   minimum:
   - `provider`: the anti-cheat provider identifier (non-empty string).
   - `integration_mode`: must be `wrap-only`.
3. The factory output bundle is staged for D-SEC gate evaluation and BC-1.15.002 lint.

## Postconditions

1. **PC1 — Provider declared non-empty:** `anti_cheat_config.provider` is present and
   non-empty. Absence causes `E-ANTICH-003`.
2. **PC2 — Provider in allowed set:** `provider` value (lowercased) is one of:
   `eac`, `eos`, `battleye`. Any other value causes `E-ANTICH-001`.
3. **PC3 — Kernel-anomaly provider unconditionally rejected:** If `provider` resolves
   (case-insensitive, after normalizing hyphens and underscores) to any of:
   `vanguard`, `riot-vanguard`, `vgk`, `riot_vanguard` — the factory emits
   `E-ANTICH-002` and blocks all scaffolding generation. This rejection is not
   overridable by any flag, config value, or agent instruction. Riot Vanguard is
   not licensable (DI-010) and uses a kernel-anomaly architecture incompatible
   with factory scope.
4. **PC4 — Generated scaffolding passes BC-1.15.002 lint:** All factory-generated
   integration scaffolding passes the kernel-mode pattern scan defined in
   BC-1.15.002. No ring-0 driver artifacts, kernel-mode entry points, or kernel
   binary blobs appear in the output bundle.
5. **PC5 — Wrap-only mode enforced:** `integration_mode` must be `wrap-only`. The
   factory generates only user-mode SDK wrapper scaffolding that calls the
   provider's user-mode API surface. The factory never authors the provider service
   binary or kernel driver.

## Invariants

1. **INV-1:** A game with `genre-profile.esports_enabled: true` without a declared
   `anti_cheat_config.provider` is a conformance failure — not a degraded state.
   D-SEC is BLOCKED if the provider is absent for an in-scope game.
2. **INV-2:** Riot Vanguard (and all `vgk`/`riot-vanguard`/`riot_vanguard` aliases)
   is NEVER a valid provider value. This constraint is not overridable by any flag,
   config, runtime override, or agent instruction. It is a factory-level invariant
   enforcing DI-010 (Kernel Anti-Cheat Is Never Autonomously Authored) and
   ADR-0008 (kernel-anomaly provider rejection policy).
3. **INV-3:** The factory always operates in wrap-only mode for anti-cheat
   integration; the factory never authors or ships the provider's binary.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `anti_cheat_config.provider: "eac"` (Easy Anti-Cheat) | PASS: provider in allowed set; wrap-only EAC SDK scaffolding generated |
| EC-002 | `anti_cheat_config.provider: "EOS"` (case variant of EOS) | PASS: lowercased "eos" is in allowed set |
| EC-003 | `anti_cheat_config.provider: "BattlEye"` (case variant) | PASS: lowercased "battleye" is in allowed set |
| EC-004 | `anti_cheat_config.provider: "riot-vanguard"` | `E-ANTICH-002` emitted; scaffolding generation BLOCKED; rejection unconditional |
| EC-005 | `anti_cheat_config.provider: "vgk"` (vgk.sys alias) | `E-ANTICH-002` emitted; scaffolding generation BLOCKED; vgk is a Riot Vanguard alias |
| EC-006 | `anti_cheat_config.provider: "riot_vanguard"` (underscore variant) | `E-ANTICH-002` emitted; scaffolding generation BLOCKED |
| EC-007 | `anti_cheat_config.provider: "custom-ac"` (custom provider not in allowed set) | `E-ANTICH-001` emitted; D-SEC BLOCKED — only {eac, eos, battleye} are valid |
| EC-008 | `anti_cheat_config` absent for `esports_enabled: true` game | `E-ANTICH-003` emitted; D-SEC BLOCKED — provider declaration is required |
| EC-009 | `integration_mode: "full-service"` declared | BLOCKED: only `wrap-only` is a valid integration mode; schema validation fails |
| EC-010 | Genre profile: `esports_enabled: false` (esports lane inactive) | Anti-cheat config not required; this BC is INAPPLICABLE for the game |
| EC-011 | PRD or config requests kernel-level Vanguard despite `anti_cheat_config` | Factory rejects with `E-ANTICH-002`; this is not overridable by instruction |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| `esports_enabled: true`; `provider: "eac"`; `integration_mode: "wrap-only"` | D-SEC anti-cheat sub-predicate passes; wrap-only EAC scaffolding generated; BC-1.15.002 lint passes | happy-path |
| `esports_enabled: true`; `provider: "eos"`; `integration_mode: "wrap-only"` | D-SEC sub-predicate passes; EOS SDK wrapper generated | happy-path |
| `esports_enabled: true`; `provider: "battleye"`; `integration_mode: "wrap-only"` | D-SEC sub-predicate passes; BattlEye wrapper generated | happy-path |
| `esports_enabled: true`; `provider: "riot-vanguard"` | `E-ANTICH-002` emitted; scaffolding BLOCKED; no override possible | error |
| `esports_enabled: true`; `provider: "vgk"` | `E-ANTICH-002` emitted; vgk.sys alias detected; BLOCKED | error |
| `esports_enabled: true`; `provider: "vanguard"` | `E-ANTICH-002` emitted; Vanguard alias detected; BLOCKED | error |
| `esports_enabled: true`; `provider: "custom-cheat-guard"` | `E-ANTICH-001` emitted; provider not in {eac, eos, battleye} | error |
| `esports_enabled: true`; `anti_cheat_config` absent | `E-ANTICH-003` emitted; D-SEC BLOCKED | error |
| `esports_enabled: false` | BC INAPPLICABLE; no anti-cheat check performed | happy-path |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-TBD-326 | For every game with `genre-profile.esports_enabled: true`, `anti_cheat_config.provider` is in `{eac, eos, battleye}` (case-insensitive) | Static lint: provider value validation over genre profile + anti_cheat_config document |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 |
| Capability Anchor Justification | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 — the anti-cheat integration adapter is part of the esports lane within genre-gated optional lanes; `genre-profile.esports_enabled: true` is the sole schema-valid activation signal (BC-13.01.001); it enforces the provider allowlist as a lane-activation precondition |
| L2 Domain Invariants | DI-010 (Kernel Anti-Cheat Is Never Autonomously Authored — this BC enforces ADR-0008 as the competitive-lane sub-enforcement of DI-010); DI-012 (Every ContractArtifact Has a Declared Validation Method) |
| Architecture Module | SS-11 (Genre-Gated Lanes); ADR-0008 (anti-cheat provider policy); D-SEC sub-predicate 2 (anti-cheat integration conformance) |
| Risk Source | R-017 (kernel anti-cheat, post-CrowdStrike; kernel AC beyond factory scope) |
| Stories | S-TBD (filled by story-writer) |

## Related BCs

- BC-1.15.002 — depends on (generated scaffolding must pass kernel-mode lint; this BC delegates ring-0 pattern scanning to BC-1.15.002)
- BC-13.01.001 — depends on (competitive-multiplayer lane must be active per genre profile validation)
- BC-7.11.001 — evaluated by (D-SEC dimension evaluation includes this BC as a required signal)
- BC-13.03.005 — sibling (both are D-SEC sub-predicates; BC-13.03.005 covers moderation pipeline; this BC covers anti-cheat provider)

## Architecture Anchors

- `specs/architecture/adrs/ADR-0008-anti-cheat-provider-policy.md` — anti-cheat allowed-provider policy (EAC, EOS, BattlEye; Vanguard rejected)
- `specs/architecture/ARCH-INDEX.md` §F42-01 — anti-cheat security sub-predicate
- `specs/architecture/adrs/ADR-0006-11-dimension-convergence-model.md` §D-SEC

## Story Anchor

S-TBD — Anti-cheat integration adapter gate for esports-lane games

## VP Anchors

- VP-TBD-326 — Provider always in allowed set (lint)
