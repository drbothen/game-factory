---
document_type: behavioral-contract
level: L3
version: "1.4"
status: draft
producer: product-owner
timestamp: 2026-06-08T00:00:00Z
phase: 1d
inputs:
  - .factory/specs/domain-spec/capabilities.md
  - .factory/specs/domain-spec/invariants.md
  - .factory/planning/research/aaa/AAA-RECONCILIATION.md
input-hash: "[compute via bin/compute-input-hash at pipeline ingest]"
traces_to: domain-spec/L2-INDEX.md
origin: greenfield
subsystem: SS-11
capability: CAP-013
priority: P2
lifecycle_status: active
introduced: v0.1.0-prd-rev-1
modified:
  - version: "1.1"
    date: 2026-06-08
    reason: "Pass-9 adversarial fix I-2: note that BC-10.01.001 now reads genre-profile.nft_mechanics for the DI-011 seam guard cross-check; add BC-10.01.001 to Related BCs as downstream consumer"
  - version: "1.2"
    date: 2026-06-08
    by: product-owner
    reason: "Pass-10 I-3: replace non-canonical AMBER with DEGRADED-PENDING for cert_preflight dimension status per methodology-layer.md §3.1 canonical enum {GREEN, DEGRADED, DEGRADED-PENDING, BLOCKED}."
  - version: "1.3"
    date: 2026-06-08
    by: product-owner
    reason: "F37-02: guard PC §1 so schema-default injection applies only to ABSENT fields (not explicit true values); add PC §1b for EC-002 explicit-true-without-spec REJECT path (E-GENRE-004); postconditions now cover all edge cases without falsification"
  - version: "1.4"
    date: 2026-06-10
    by: product-owner
    reason: "F51-01: fix mis-anchor in PC §2 console cert-preflight routing — replace BC-9.03.001/002/003 (Steam/itch.io/mobile adapters, not console) with BC-9.06.001 (Human-Gated Console Cert Sign-Off, the actual console submission path). F51-02: reconcile nft_blockchain_policy interface — remove runtime check-slot injection claim; instead state the slot is statically declared in console cert-preflight-config per BC-9.01.001 (static-slot reconciliation). Related BCs line updated to match."
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# BC-13.01.004: Genre Profile Default Enforces NFT/Web3 Off-By-Default (DI-011)

## Description

When a game project is initialized without an explicit `business-model-spec` declaration
enabling NFT, blockchain token, or web3 mechanics, the factory must apply the
`nft_mechanics: false` and `web3_enabled: false` defaults in the genre profile. Activating
NFT/web3 mechanics requires an explicit opt-in declaration in `business-model-spec` with
acknowledgment of the PEGI-18 minimum rating consequence. This BC enforces DI-011 at the
genre profile validation level as a genre-gated activation gate.

Enforcement is done both at genre profile schema validation time and at pipeline
initialization — any downstream lane or artifact generator that checks the genre profile
will receive these defaults if they were not explicitly overridden.

## Preconditions

1. A game project is being initialized with a genre profile document.
2. The genre profile is validated against the canonical genre-profile schema.
3. The project's `business-model-spec` is either absent (new project default) or
   present (explicit declaration with acknowledged consequences).

## Postconditions

1. When no `business-model-spec` is present in the project **AND the genre profile does
   not explicitly declare `nft_mechanics`, `web3_enabled`, or `blockchain_token` as `true`**
   (i.e., fields are absent or already false):
   - The validated genre profile contains `nft_mechanics: false`
   - The validated genre profile contains `web3_enabled: false`
   - The validated genre profile contains `blockchain_token: false`
   - These defaults are injected by the schema defaulting mechanism, not left absent
   - **Scope:** Schema-default injection applies ONLY to ABSENT or already-false fields.
     If a field is explicitly set to `true`, PC §1b applies instead.

1b. When no `business-model-spec` is present **AND the genre profile explicitly declares
    `nft_mechanics: true` OR `web3_enabled: true` OR `blockchain_token: true`**:
   - Validation FAILS with `E-GENRE-004`
   - Pipeline is blocked; no validated genre profile is produced
   - A human task is surfaced: "NFT/Web3 field explicitly set to true but no
     business-model-spec found — create business-model-spec with pegi_18_acknowledged:true
     to activate NFT/web3 mechanics"
   - The schema defaulting mechanism MUST NOT coerce explicit `true` values to `false`;
     the opt-in gate (E-GENRE-004) is the correct response

2. When a `business-model-spec` is present and declares `nft_mechanics: true` or
   `web3_enabled: true`:
   - The genre profile activates the corresponding field
   - The validation gate checks that `business-model-spec.pegi_18_acknowledged: true`
     is set; if absent, validation fails with `E-GENRE-005`
   - A human-gated review task is surfaced: "NFT/Web3 mechanics activated — PEGI 18
     minimum rating applies; legal review required before distribution"
   - **Console platform cert-preflight routing (I8):** For any `target_platform` in
     `{xbox, psn, switch}`, the console `cert-preflight-config` statically declares a
     `nft_blockchain_policy` check slot (per BC-9.01.001 INV-1 — checks are config-declared,
     not injected at runtime). When NFT/web3 is active, that slot's result MUST be
     `REQUIRES_HUMAN_REVIEW` with message: "NFT/web3 mechanics declared — verify platform
     holder NFT/blockchain policy compliance before cert submission (Sony, Microsoft, and
     Nintendo each maintain independent policies that may block certification independent
     of age rating)." When NFT/web3 is inactive (`nft_mechanics: false`, `web3_enabled:
     false`), the slot result is `SKIP` (inactive feature, not applicable). The
     `convergence-report.dimensions.cert_preflight` is set to `DEGRADED-PENDING` (not PASS)
     until the `nft_blockchain_policy` slot is cleared by human review. This signal must
     appear **before** the human-gated console cert submission step (BC-9.06.001).
3. The factory never generates NFT smart contract code, web3 wallet integration,
   or blockchain token minting/transfer scaffolding without an explicit opt-in declaration.
4. The inactive-lane zero-artifact guarantee (BC-13.01.002) covers NFT/web3 lane
   artifacts: when `nft_mechanics: false` and `web3_enabled: false`, no NFT/web3
   artifacts are generated anywhere in the pipeline.

## Invariants

1. NFT mechanics and web3 features are always `false` in the default genre profile — no
   partial default or opt-out-required default is permitted.
2. This default is not overridable via flag, agent instruction, or pipeline parameter
   without an explicit `business-model-spec` document in the project repository.
3. The PEGI-18-acknowledged check is non-bypassable when NFT/web3 is activated.
4. Riot Vanguard and similar non-licensable anti-cheat dependencies do not become activated
   via any web3/NFT flag (these are separate concerns; the web3 flag is not a platform
   privilege escalation path).

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | New project initialized with empty genre profile | Schema validator injects `nft_mechanics: false`, `web3_enabled: false`, `blockchain_token: false` as defaults; validation passes |
| EC-002 | Genre profile explicitly sets `nft_mechanics: true` without `business-model-spec` | Validation fails with `E-GENRE-004`; human task surfaced requiring `business-model-spec` creation |
| EC-003 | Genre profile has `business-model-spec` with `nft_mechanics: true` but `pegi_18_acknowledged: false` | Validation fails with `E-GENRE-005`; pipeline blocked until acknowledgment is provided |
| EC-004 | Genre profile has `business-model-spec` with both `nft_mechanics: true` and `pegi_18_acknowledged: true`, Steam target only | Validation passes; NFT lane activated; human-gated legal review task surfaced; PEGI 18 minimum injected into compliance checklist; no console cert-preflight routing needed (non-console target) |
| EC-005 | Project attempts to merge a PR that adds `web3_enabled: true` via pipeline parameter (not business-model-spec) | Gate fails; E-GENRE-004 emitted; explicit `business-model-spec` document required in repo |
| EC-006 | Project uses cosmetic blockchain feature (e.g., NFT-based avatar frame) | Treated as `nft_mechanics: true` — PEGI-18 consequence applies to any NFT mechanic regardless of cosmetic vs. functional nature |
| EC-007 | Project has `nft_mechanics: true`, targets both `steam` and `psn` | Console cert-preflight routing fires for `psn`; Steam target unaffected by console routing; both the PEGI-18 legal review task and the `psn` cert-preflight REQUIRES_HUMAN_REVIEW slot are surfaced |
| EC-008 | Project has `nft_mechanics: true`, targets `switch` | Nintendo NFT/blockchain policy check added to Switch cert-preflight; `cert_preflight` dimension = DEGRADED-PENDING until human review confirms Nintendo policy compliance |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| Empty genre profile, no `business-model-spec` | Validated genre profile has `nft_mechanics: false`, `web3_enabled: false`, `blockchain_token: false` | happy-path |
| Genre profile with `genre: competitive-fps`, no `business-model-spec` | Same defaults injected; esports lane activatable; NFT/web3 remain false | happy-path |
| Genre profile `nft_mechanics: true`, no `business-model-spec` | Validation fails, `E-GENRE-004`, human task created — PC §1b: explicit true without spec is REJECTED, NOT coerced to false | error |
| `business-model-spec` present, `nft_mechanics: true`, `pegi_18_acknowledged: false` | Validation fails, `E-GENRE-005` | error |
| `business-model-spec` present, `nft_mechanics: true`, `pegi_18_acknowledged: true` | Validation passes; NFT lane active; human review task surfaced; PEGI 18 minimum set | opt-in |
| Direct API call sets `web3_enabled: true` without `business-model-spec` | Gate rejects; `E-GENRE-004`; cannot bypass via API parameter | security |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-TBD-062 | Every initialized genre profile has explicit `nft_mechanics` and `web3_enabled` fields (no absent defaults) | Schema validation: fields must be present and boolean |
| VP-TBD-063 | NFT/web3 fields are `false` in every project without `business-model-spec` opt-in | Property-based test: generate N random valid genre profiles without business-model-spec; assert all have these fields false |
| VP-TBD-064 | PEGI-18 minimum is set in compliance checklist for any project with `nft_mechanics: true` | Integration test: compliance-checklist generation with opt-in project; assert PEGI 18 minimum present |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 |
| Capability Anchor Justification | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 — NFT/web3 mechanics are a genre-gated optional feature lane that must default to inactive per DI-011; this BC enforces that the activation gate requires explicit opt-in, exactly what the genre-gated lane activation capability governs |
| L2 Domain Invariants | DI-011 (NFT and Web3 Mechanics Are Off by Default — THIS BC IS THE PRIMARY ENFORCEMENT OF DI-011); DI-006 (human-gated tasks are surfaced, not silently dropped — the PEGI-18 legal review task when NFT is activated) |
| Architecture Module | SS-11 (Genre-Gated Lanes) |
| Stories | S-TBD (filled by story-writer) |

## Related BCs

- BC-13.01.001 — composes with (genre profile schema validation is a precondition; this BC adds the NFT/web3 default enforcement on top of structural schema validation)
- BC-13.01.002 — composes with (inactive-lane zero-artifact guarantee; when NFT is false this BC's defaults ensure the NFT lane never activates, which BC-13.01.002 then guarantees produces no artifacts)
- BC-13.01.003 — composes with (lane activation idempotency applies after NFT/web3 defaults are confirmed)
- BC-10.01.001 — **downstream consumer** (reads `genre-profile.nft_mechanics` to perform the DI-011 seam guard cross-check against `game-metadata-spec.nft_blockchain`; any divergence emits E-COMP-012 and applies conservative PEGI-18; this BC is the authoritative source of `nft_mechanics` that BC-10.01.001 consumes)
- BC-10.02.001 — depends on (compliance-checklist generated per genre/region profile consumes the `nft_mechanics` flag to determine PEGI 18 minimum; this BC ensures the flag is always present and correct before the compliance-checklist is generated)
- **BC-9.01.001 — routes into** (console cert-preflight-config statically declares the `nft_blockchain_policy` slot; when NFT/web3 is active its result is REQUIRES_HUMAN_REVIEW; when inactive its result is SKIP; the cert-preflight report's DEGRADED-PENDING state persists until human review clears the slot)
- **BC-9.06.001 — gates into** (the NFT/web3 DEGRADED-PENDING cert_preflight signal must be resolved before the human-gated console cert submission step that BC-9.06.001 governs)

## Architecture Anchors

- `specs/domain-spec/invariants.md` §DI-011 — source of invariant
- `planning/research/aaa/AAA-RECONCILIATION.md §6.3` — NFT/web3 off-by-default rationale
- `planning/research/aaa/AAA-RECONCILIATION.md §12 R-013` — PEGI 2026 NFT → PEGI 18 trigger
- `specs/architecture/subsystem-decomposition.md` §SS-11 — Genre-Gated Lanes subsystem
