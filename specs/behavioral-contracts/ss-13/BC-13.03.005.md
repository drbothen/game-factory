---
document_type: behavioral-contract
level: L3
version: "1.1"
status: draft
producer: product-owner
timestamp: 2026-06-09T00:00:00Z
phase: 1a
inputs:
  - .factory/specs/domain-spec/capabilities.md
  - .factory/specs/domain-spec/invariants.md
  - .factory/specs/architecture/ARCH-INDEX.md
  - .factory/specs/architecture/adrs/ADR-0006-11-dimension-convergence-model.md
input-hash: "[compute via bin/compute-input-hash at pipeline ingest]"
traces_to: domain-spec/L2-INDEX.md
origin: greenfield
subsystem: SS-11
capability: CAP-013
priority: P2
lifecycle_status: active
introduced: v0.1.0-prd-pass-42
modified:
  - version: "1.1"
    date: 2026-06-09
    author: product-owner
    reason: "F44-01: reconcile trigger vocabulary to schema-valid signals — replace non-schema ugc_enabled/chat_enabled with genre-profile.modding_enabled (UGC lane) and game-metadata-spec.user_to_user_communication (chat); CSAM gate is now reachable via valid genre profiles"
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# BC-13.03.005: Moderation Pipeline Contract — UGC/Chat CSAM Reporting and Fail-Closed Enforcement

## Description

Any game with UGC or chat features enabled must declare and wire a conformant
moderation pipeline before D-SEC passes. The pipeline must have a declared provider,
a CSAM detection method, and a configured NCMEC CyberTipline report path (18 U.S.C.
§2258A). The moderation pipeline is fail-closed: if the pipeline is unavailable, UGC
is BLOCKED — not silently accepted. Any CSAM-suspect content triggers a human review
queue (DI-006) and is never auto-deleted without a human-reviewed evidence record.

UGC presence is determined by the schema-valid genre-profile field `modding_enabled: true`
(the UGC/modding lane per BC-13.01.001). Chat presence is determined by
`game-metadata-spec.user_to_user_communication: true` (the canonical IARC/PEGI chat
signal per BC-10.01.001). Both signals are schema-validated before this gate is evaluated.

## Preconditions

1. The genre profile (validated per BC-13.01.001) declares `modding_enabled: true`
   (UGC/modding lane active) OR the game-metadata-spec declares
   `user_to_user_communication: true` (chat feature active) for the target game.
2. A `moderation-pipeline-manifest` document is present in the game artifact set,
   declaring at minimum:
   - `provider`: the moderation service provider identifier (non-empty string).
   - `csam_detection_method`: the technical detection approach used.
   - `ncmec_report_path`: the configured NCMEC CyberTipline endpoint or integration.
3. The factory output bundle is staged for D-SEC gate evaluation.

## Postconditions

1. **PC1 — Provider declared non-empty:** `moderation-pipeline-manifest.provider` is
   present and non-empty. An absent or empty-string provider causes `E-TMOD-001`
   and blocks D-SEC.
2. **PC2 — CSAM detection method declared:** `csam_detection_method` is one of:
   `PhotoDNA`, `C2PA scan`, `hash-match`. Any other value (or absence) causes
   `E-TMOD-001`.
3. **PC3 — NCMEC report path non-null:** `ncmec_report_path` is declared and non-null.
   The NCMEC CyberTipline integration must be configured. Absence causes `E-TMOD-002`.
4. **PC4 — Fail-closed:** If the moderation pipeline is declared unavailable or
   degraded at runtime, UGC ingestion is BLOCKED. Silent acceptance of UGC when the
   pipeline is unavailable is a defect. Any degraded pipeline state must be declared
   explicitly in the manifest's `degraded_behavior` field; an undeclared degraded
   state is treated as `block_ugc` (most restrictive default). A `degraded_behavior:
   pass_through` (fail-open) value causes `E-TMOD-003` and blocks D-SEC.
5. **PC5 — Human review queue for CSAM-suspect:** When a CSAM-suspect signal is
   raised, the factory surfaces a human review task (DI-006) with the suspect
   content ID and detection evidence. The content is quarantined pending human
   review. Auto-deletion without a human-reviewed evidence record is a hook-
   detectable defect.

## Invariants

1. **INV-1:** No game with `genre-profile.modding_enabled: true` or
   `game-metadata-spec.user_to_user_communication: true` may have a deployed factory
   output bundle without a declared and structurally valid `moderation-pipeline-manifest`.
   Absence of the manifest for an in-scope game is a D-SEC BLOCKED condition — not a
   degraded state.
2. **INV-2:** Moderation pipeline absence for in-scope games equals D-SEC BLOCKED.
   There is no graceful degradation path for a missing moderation pipeline on
   UGC/chat-enabled games.
3. **INV-3:** Human review tasks for CSAM-suspect content are governed by DI-006 —
   they are surfaced, checklisted, and must not be suppressed or silently dropped.

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Genre profile has `modding_enabled: false` AND game-metadata-spec has `user_to_user_communication: false` (or absent) | Moderation pipeline manifest is not required; this BC is INAPPLICABLE for the game |
| EC-002 | `moderation-pipeline-manifest` present but `provider` is empty string | `E-TMOD-001` emitted; D-SEC gate BLOCKED |
| EC-003 | `csam_detection_method` is `manual-review-only` (not in allowed set) | `E-TMOD-001` emitted; D-SEC gate BLOCKED; must use one of {PhotoDNA, C2PA scan, hash-match} |
| EC-004 | `ncmec_report_path` is `null` or absent | `E-TMOD-002` emitted; D-SEC gate BLOCKED |
| EC-005 | `degraded_behavior: pass_through` declared in manifest | `E-TMOD-003` emitted; D-SEC gate BLOCKED — fail-open moderation is not a valid configuration |
| EC-006 | `degraded_behavior` field absent; pipeline is unavailable at runtime | Treated as `block_ugc` by default; UGC blocked; no E-TMOD error (default is correct behavior) |
| EC-007 | CSAM-suspect flag raised by PhotoDNA; factory attempts to auto-delete content | BLOCKED: factory must quarantine and surface human review task (DI-006); auto-deletion without human-reviewed evidence record is a defect |
| EC-008 | Game has `user_to_user_communication: true` in game-metadata-spec only (no UGC/modding lane); moderation manifest absent | `E-TMOD-001` emitted; D-SEC gate BLOCKED — chat-only games (via game-metadata-spec) are in scope for this BC |
| EC-009 | Genre profile `modding_enabled: true` (UGC lane active); moderation manifest absent | `E-TMOD-001` emitted; D-SEC gate BLOCKED — UGC lane activation requires moderation pipeline |

## Canonical Test Vectors

| Input | Expected Output | Category |
|-------|----------------|----------|
| Genre profile: `modding_enabled: true`; manifest present with `provider: "hive-moderation"`, `csam_detection_method: "PhotoDNA"`, `ncmec_report_path: "https://ncmec.example/report"` | D-SEC moderation sub-predicate passes; no error | happy-path |
| game-metadata-spec: `user_to_user_communication: true`; manifest present with valid provider + PhotoDNA + ncmec_report_path | D-SEC moderation sub-predicate passes; no error | happy-path |
| Genre profile: `modding_enabled: true`; manifest present; `provider: ""` (empty) | `E-TMOD-001` emitted; D-SEC BLOCKED | error |
| Genre profile: `modding_enabled: true`; manifest present; `csam_detection_method: "keyword-filter"` | `E-TMOD-001` emitted — keyword-filter not in allowed set | error |
| Genre profile: `modding_enabled: true`; manifest present; `ncmec_report_path: null` | `E-TMOD-002` emitted; D-SEC BLOCKED | error |
| Genre profile: `modding_enabled: true`; manifest present; `degraded_behavior: "pass_through"` | `E-TMOD-003` emitted; D-SEC BLOCKED — fail-open rejected | error |
| Genre profile: `modding_enabled: false`; game-metadata-spec: `user_to_user_communication: false` | Moderation manifest not required; BC INAPPLICABLE; no error | happy-path |
| CSAM-suspect signal raised during UGC ingest | Human review task surfaced (DI-006); content quarantined; no auto-delete | happy-path |
| game-metadata-spec: `user_to_user_communication: true`; moderation manifest absent | `E-TMOD-001` emitted; D-SEC BLOCKED | error |

## Verification Properties

| VP-NNN | Property | Proof Method |
|--------|----------|-------------|
| VP-TBD-324 | For every game with `genre-profile.modding_enabled: true` or `game-metadata-spec.user_to_user_communication: true`, a structurally valid `moderation-pipeline-manifest` is present before D-SEC passes | Static lint: manifest presence + schema validation over output bundle |
| VP-TBD-325 | A pipeline declared unavailable with `degraded_behavior: pass_through` is rejected at D-SEC gate (fail-closed; UGC blocked when CSAM detection unavailable) | Integration test: inject `degraded_behavior: pass_through`; assert E-TMOD-003 fired and D-SEC BLOCKED |

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 |
| Capability Anchor Justification | CAP-013 ("Genre-Gated Optional Lane Activation") per capabilities.md §CAP-013 — the moderation pipeline contract is a safety gate within the UGC/modding lane (activated by `genre-profile.modding_enabled: true`) and for chat-enabled games (signaled by `game-metadata-spec.user_to_user_communication: true`); both signals are schema-valid and precede D-SEC evaluation |
| L2 Domain Invariants | DI-006 (Human-Gated Tasks Are Surfaced, Not Silently Dropped — CSAM human review queue is a DI-006 instance); DI-012 (Every ContractArtifact Has a Declared Validation Method — this BC's validation method is static lint + integration test) |
| Architecture Module | SS-11 (Genre-Gated Lanes); D-SEC sub-predicate 3 (moderation pipeline wired for UGC/chat games) |
| Risk Source | R-017 extension (CSAM/NCMEC reporting obligation for UGC games; 18 U.S.C. §2258A) |
| Stories | S-TBD (filled by story-writer) |

## Related BCs

- BC-13.03.001 — depends on (mod-API lane must be active; moderation applies to UGC lane artifacts)
- BC-13.03.002 — depends on (UGC content schema validation precedes moderation pipeline check)
- BC-7.11.001 — evaluated by (D-SEC dimension evaluation includes this BC as a required signal)
- BC-1.15.003 — sibling (both are D-SEC sub-predicates; BC-1.15.003 covers output-bundle secrets; this BC covers moderation pipeline wiring)

## Architecture Anchors

- `specs/architecture/ARCH-INDEX.md` §F42-MOD — moderation pipeline security sub-predicate
- `specs/architecture/adrs/ADR-0006-11-dimension-convergence-model.md` §D-SEC — D-SEC convergence dimension fail-closed signal

## Story Anchor

S-TBD — Moderation pipeline contract gate for UGC/modding-lane and chat games

## VP Anchors

- VP-TBD-324 — Moderation manifest present for UGC/chat games (lint)
- VP-TBD-325 — Fail-closed: CSAM detect failure blocks UGC
