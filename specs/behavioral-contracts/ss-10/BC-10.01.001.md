---
document_type: behavioral-contract
level: L3
version: "1.1"
status: active
producer: product-owner
timestamp: 2026-06-08T00:00:00Z
phase: 1d
inputs:
  - .factory/specs/domain-spec/capabilities.md
  - .factory/specs/domain-spec/invariants.md
  - .factory/planning/research/aaa/ratings-legal-compliance.md
  - .factory/planning/research/aaa/AAA-RECONCILIATION.md
input-hash: "[compute via bin/compute-input-hash at pipeline ingest]"
traces_to: domain-spec/capabilities.md
origin: greenfield
subsystem: SS-08
capability: CAP-010
priority: P1
lifecycle_status: active
introduced: v0.1.0
modified:
  - version: "1.1"
    date: 2026-06-08
    reason: "Pass-9 adversarial fix I-2: add nft_blockchain/nft_mechanics cross-field consistency check (DI-011 seam guard); register E-COMP-012; ensure ratings-submission-manifest reflects PEGI-18 via either NFT activation source; extend EC-001 to cover cross-artifact divergence"
deprecated: null
deprecated_by: null
replacement: null
retired: null
removed: null
removal_reason: null
---

# BC-10.01.001: IARC Objective-Questionnaire Auto-Fill from Game Metadata

## Description

The factory auto-fills the objective (machine-answerable) questions of the IARC
questionnaire from verifiable game metadata: the economy graph, content-descriptor tags,
feature flags, and monetization spec. Objective questions are those answerable from
declared game data without content-intensity judgment — presence of IAP, random-paid items,
user communication, location data, NFT/blockchain, gambling simulation, and PEGI-2026
interactive-risk features. Content-intensity questions (violence level, sexual content,
strong language) are flagged `REQUIRES_HUMAN_ANSWER`. The output is a
`ratings-submission-manifest` that drives storefront IARC submission. The ratings submission
terminal step is human-gated (BC-10.06.001). Regulatory source: IARC (globalratings.com).

## Preconditions

1. A `game-metadata-spec` exists for the game with declared fields:
   - `in_app_purchases: bool`
   - `random_paid_items: bool` (loot boxes, gacha, card packs)
   - `user_to_user_communication: bool`
   - `location_shared: bool`
   - `unrestricted_internet: bool`
   - `simulated_gambling: bool`
   - `real_money_gambling: bool`
   - `nft_blockchain: bool`
   - `time_limited_offers: bool`
   - `punitive_play_by_appointment: bool` (lose content/progress for not logging in)
   - `content_descriptors[]`: list of content tags (from `content-descriptor-contract`)
2. The factory has access to the declared IARC questionnaire schema version.
3. The PEGI 2026 interactive-risk category rule engine is loaded (effective June 2026).
4. **DI-011 seam guard (I-2):** The factory has read access to the project's validated
   `genre-profile` document (produced by BC-13.01.004) to cross-check
   `genre-profile.nft_mechanics` against `game-metadata-spec.nft_blockchain` before
   computing the PEGI-2026 minimum age. If the genre-profile is unavailable, the factory
   treats this as a conservative case: `nft_blockchain` value from `game-metadata-spec`
   is used as the sole NFT activation signal and the absence of the genre-profile is noted
   as a `compliance_flags` entry.

## Behavior

1. For each objective IARC questionnaire question, the factory maps from `game-metadata-spec`:

   | IARC Question | Source field | Mapping |
   |---------------|-------------|---------|
   | In-App Purchases present? | `in_app_purchases` | Direct bool |
   | In-App Purchases include random/paid items? | `random_paid_items` | Direct bool |
   | User-to-User communication present? | `user_to_user_communication` | Direct bool |
   | Is user communication unrestricted? | `user_to_user_communication AND NOT has_block_report_tools` | Derived |
   | Location data shared? | `location_shared` | Direct bool |
   | Unrestricted internet access? | `unrestricted_internet` | Direct bool |
   | Simulated gambling? | `simulated_gambling` | Direct bool |
   | Real money gambling? | `real_money_gambling` | Direct bool |
   | NFT/blockchain mechanics? | `nft_blockchain` | Direct bool |
   | Time-limited/quantity-limited offers? | `time_limited_offers` | Direct bool |
   | Punitive play-by-appointment? | `punitive_play_by_appointment` | Direct bool |

2. For content-intensity questions (violence level, sexual content, drug references,
   strong language, horror, discrimination): emit `REQUIRES_HUMAN_ANSWER` with the
   question text and a content-classifier advisory note (non-authoritative).

3. Compute PEGI-2026 predicted minimum age from objective data:
   - `random_paid_items: true` → minimum_pegi ≥ 16
   - `nft_blockchain: true` → minimum_pegi = 18 (hard override)
   - `time_limited_offers: true` → minimum_pegi ≥ 12
   - `punitive_play_by_appointment: true` → minimum_pegi ≥ 12
   - `user_to_user_communication: true AND NOT has_block_report_tools: true` → minimum_pegi = 18
   - (Higher value from all rules wins; default minimum_pegi = 3)

4. **DI-011 seam guard — cross-field consistency check (I-2):** Before emitting the
   manifest, the factory reads `genre-profile.nft_mechanics` (produced by BC-13.01.004)
   and compares it with `game-metadata-spec.nft_blockchain`:

   | `nft_mechanics` | `nft_blockchain` | Action |
   |-----------------|------------------|--------|
   | `true` | `true` | Consistent NFT-active: apply PEGI-18 hard override (normal flow) |
   | `false` | `false` | Consistent NFT-inactive: no override (normal flow) |
   | `true` | `false` | **Inconsistency:** emit E-COMP-012; apply conservative PEGI-18 hard override; add `compliance_flags: ["NFT_PEGI18_OVERRIDE: DI-011 — nft_mechanics=true in genre-profile but nft_blockchain=false in game-metadata-spec; PEGI 18 applied conservatively — reconcile fields before storefront submission"]` |
   | `false` | `true` | **Inconsistency:** emit E-COMP-012; apply conservative PEGI-18 hard override; add `compliance_flags: ["NFT_PEGI18_OVERRIDE: DI-011 — nft_blockchain=true in game-metadata-spec but nft_mechanics=false in genre-profile; PEGI 18 applied conservatively — reconcile fields before storefront submission"]` |

   The E-COMP-012 error is non-blocking for manifest emission (the manifest IS emitted
   with the conservative PEGI-18 and the flag) but the `ratings-submission-manifest` will
   have `status: "auto_filled_partial_with_warnings"` and the human-gated submission step
   (BC-10.06.001) must resolve the inconsistency before final IARC submission.

   **Rationale:** DI-011 states NFT → PEGI 18. When the two NFT activation signals diverge,
   there is a real risk of an under-rated storefront submission (R-013). The conservative
   fail-closed posture ensures the `ratings-submission-manifest` always reflects PEGI-18
   whenever ANY active NFT signal is present, regardless of which artifact it comes from.

5. Emit `ratings-submission-manifest` (JSON):
   ```json
   {
     "game_id": "<id>",
     "build_version": "<version>",
     "iarc_questionnaire_version": "<ver>",
     "manifest_timestamp": "<ISO-8601>",
     "answers": [
       {"question_id": "<qid>", "answer": true|false|"REQUIRES_HUMAN_ANSWER", "source_field": "<field>", "advisory_note": "<optional>"}
     ],
     "pegi_2026_predicted_minimum_age": 3|7|12|16|18,
     "pegi_2026_rating_triggers": ["<list of triggered rules>"],
     "human_answer_required_count": N,
     "status": "auto_filled_partial"
   }
   ```

6. If NFT is active (either `nft_blockchain: true` in `game-metadata-spec` OR
   `nft_mechanics: true` in `genre-profile`, or both): add a compliance flag in the manifest:
   `"compliance_flags": ["NFT_PEGI18_OVERRIDE: DI-011 — NFT triggers PEGI 18 minimum"]`
   If fields are inconsistent (step 4 detected divergence), also add the E-COMP-012 flag per
   step 4, and set `"status": "auto_filled_partial_with_warnings"` instead of
   `"status": "auto_filled_partial"`.

## Postconditions

- `ratings-submission-manifest` exists at `.factory/artifacts/compliance/<game_id>/
  ratings-submission-manifest-<build_version>.json`.
- Every objective question has a `true`/`false` answer with a `source_field` trace.
- Every content-intensity question has `answer: "REQUIRES_HUMAN_ANSWER"`.
- `pegi_2026_predicted_minimum_age` is computed and reflects all applicable rule triggers.
- No content-intensity question is answered as `true`/`false` by the factory.

## Invariants

- INV-1: The factory answers ONLY objective questions. Content-intensity judgment is always
  `REQUIRES_HUMAN_ANSWER` — no content classifier output may substitute for this.
- INV-2 (DI-011): **PEGI-18 is applied whenever EITHER `nft_blockchain: true` (in
  `game-metadata-spec`) OR `nft_mechanics: true` (in `genre-profile`) — not only when
  both agree.** The `ratings-submission-manifest` always contains a PEGI-18
  `compliance_flags` entry whenever any NFT signal is active. Neither signal may be
  silently ignored. Divergence between the two sources is surfaced as E-COMP-012 (non-blocking
  for the manifest; blocking for final IARC submission via BC-10.06.001).
- INV-3: The questionnaire version is recorded in the manifest; questionnaire schema changes
  require a new manifest generation run.
- INV-4: The `ratings-submission-manifest` is an input to the human-gated submission step
  (BC-10.06.001); it does not itself submit to IARC or any storefront.

## Edge Cases

| EC-ID | Scenario | Expected Result |
|-------|----------|----------------|
| EC-001 | `game-metadata-spec` declares `nft_blockchain: false` but `content_descriptors` includes `NFT_MECHANIC` tag | Inconsistency flagged: `compliance_flags: ["METADATA_INCONSISTENCY: nft_blockchain=false but content_descriptors includes NFT_MECHANIC — human review required"]`; `nft_blockchain` treated as `true` conservatively |
| EC-002 | `user_to_user_communication: true` and `has_block_report_tools: true` | `unrestricted_internet_communications: false`; PEGI 18 unrestricted-comms rule NOT triggered |
| EC-003 | `user_to_user_communication: true` and `has_block_report_tools` not declared | Treated conservatively as `has_block_report_tools: false`; PEGI 18 unrestricted-comms rule triggered; `compliance_flags: ["CONSERVATIVE_ASSUMPTION: has_block_report_tools not declared, treated as false"]` |
| EC-004 | All monetization features absent (solo offline game) | All IAP/gacha/NFT answers `false`; `pegi_2026_predicted_minimum_age: 3`; `human_answer_required_count: N` for content questions |
| EC-005 | Both `random_paid_items: true` AND `nft_blockchain: true` | NFT override wins (PEGI 18); `pegi_2026_rating_triggers: ["random_paid_items_pegi16", "nft_blockchain_pegi18_override"]` |
| EC-006 | Questionnaire version in config not recognized | Error `E-COMP-001`; manifest not emitted |
| EC-007 | `genre-profile.nft_mechanics: true` but `game-metadata-spec.nft_blockchain: false` (DI-011 seam divergence) | E-COMP-012 emitted; conservative PEGI-18 override applied; `compliance_flags` contains both `NFT_PEGI18_OVERRIDE: DI-011` and the field-divergence message; `status: "auto_filled_partial_with_warnings"`; human-gated submission step (BC-10.06.001) must resolve before IARC submission |
| EC-008 | Genre-profile unavailable at manifest generation time | Conservative path: `nft_blockchain` from `game-metadata-spec` used as sole NFT activation signal; `compliance_flags` entry notes genre-profile absence; no E-COMP-012 (cannot compare) |

## Canonical Test Vectors

| game-metadata-spec | genre-profile.nft_mechanics | Expected `pegi_2026_predicted_minimum_age` | Expected flags / status |
|-------------------|----------------------------|-------------------------------------------|------------------------|
| All features off | `false` | 3 | none; `status: auto_filled_partial` |
| `random_paid_items: true` | `false` | 16 | `random_paid_items_pegi16`; `status: auto_filled_partial` |
| `nft_blockchain: true` | `true` | 18 | `nft_blockchain_pegi18_override`, `NFT_PEGI18_OVERRIDE: DI-011`; `status: auto_filled_partial` |
| `time_limited_offers: true` | `false` | 12 | `time_limited_offers_pegi12`; `status: auto_filled_partial` |
| `random_paid_items: true`, `nft_blockchain: true` | `true` | 18 | both triggers; NFT wins; `status: auto_filled_partial` |
| `user_to_user_communication: true`, `has_block_report_tools: false` | `false` | 18 | `unrestricted_comms_pegi18`; `status: auto_filled_partial` |
| `nft_blockchain: false` | `true` (inconsistency) | 18 | E-COMP-012 + `NFT_PEGI18_OVERRIDE` + divergence flag; `status: auto_filled_partial_with_warnings` |
| `nft_blockchain: true` | `false` (inconsistency) | 18 | E-COMP-012 + `NFT_PEGI18_OVERRIDE` + divergence flag; `status: auto_filled_partial_with_warnings` |

## Verification Properties

- VP-COMP-001: No content-intensity IARC question is answered with `true` or `false` by the factory.
- VP-COMP-002: `pegi_2026_predicted_minimum_age` is always ≥ 18 when `nft_blockchain: true`
  OR when `genre-profile.nft_mechanics: true` — the conservative fail-closed posture applies
  to both NFT activation sources (DI-011 seam guard).
- VP-COMP-003: Every answered question in the manifest has a `source_field` trace to a
  `game-metadata-spec` field.

## Traceability

| Field | Value |
|-------|-------|
| L2 Capability | CAP-010 ("Compliance Pipeline and AI Disclosure") per capabilities.md §CAP-010 |
| Capability Anchor Justification | CAP-010 ("Compliance Pipeline and AI Disclosure") per capabilities.md §CAP-010 — this BC defines the IARC objective questionnaire auto-fill behavior, which is the primary named output of CAP-010 ("auto-fills IARC objective questionnaire answers from game metadata") |
| L2 Invariants | DI-011 (NFT triggers PEGI 18 — enforced via cross-field seam guard across `nft_blockchain` and `nft_mechanics`; E-COMP-012 guards the divergence path), DI-012 (declared validation method) |
| Research Grounding | ratings-legal-compliance.md §2.1 (IARC: single questionnaire, embedded in storefront submission, verified globalratings.com), §2.2 (PEGI 2026 interactive-risk categories, primary-verified pegi.info), §2.3 (machine-answerable vs human-judgment question split), §9 (GREEN compliance items) |

## Related BCs

- BC-10.02.001 — Compliance-Checklist Generation (consumes ratings-submission-manifest)
- BC-10.06.001 — Human-Gated Ratings Submission Terminal Step (terminal step after this BC; must resolve E-COMP-012 inconsistencies before final IARC submission)
- BC-13.01.004 — depends on (genre-profile.nft_mechanics is the authoritative DI-011 source; this BC reads that field to perform the DI-011 seam guard cross-check against game-metadata-spec.nft_blockchain)
